// Bumped with every release, and mirrored in package.json.
//
// Shown on the mode pill, logged at boot, and appended to the font URLs — because "is my iframe
// running the new bundle or a cached one" was undiagnosable without it.
export const APP_VERSION = '1.19.0';
